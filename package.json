{
  "name": "eigenorg",
  "version": "0.0.0",
  "private": true,
  "description": "Dev toolchain only — nothing npm-installed ships to the site.",
  "type": "module",
  "scripts": {
    "lint": "eslint . --no-error-on-unmatched-pattern",
    "typecheck": "tsc -p jsconfig.json --noEmit",
    "test": "node --test"
  },
  "devDependencies": {
    "@eslint/js": "^9.18.0",
    "ajv": "^8.17.1",
    "chart.js": "4.5.1",
    "eslint": "^9.18.0",
    "http-server": "^14.1.1",
    "typescript": "^5.7.3"
  }
}
